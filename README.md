# read-mul

Python script to read files with the extensions .mul and .flm, acquired by the STM scanning software "SpecsProbe.exe", which is used with the SPM 150 Aarhus by Specs.
Image data and point scan data are read as numpy arrays, therefore numpy is required.